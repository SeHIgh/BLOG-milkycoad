name: Java CICD
'on':
  workflow_dispatch: {
    }
  push:
    branches:
     - v2
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
     -
      name: Checkout repository
      uses: actions/checkout@v4
     -
      name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: adopt
        java-version: '21'
  job2:
    runs-on: ubuntu-latest
    steps: [
      ]
  job3:
    runs-on: ubuntu-latest
    steps: [
      ]
